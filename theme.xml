<theme>
	<formatVersion>4</formatVersion>

	<!-- CLON3D CYBERDECK OS v4.0 -->
	<!-- Modular theme architecture - NO Carbon dependency -->
	<!-- Each view is independently designed and maintained -->

	<!-- System Dashboard View: Completely original fullscreen interface -->
	<include>./system.xml</include>

	<!-- Game List Views: Custom carousel and selection interface -->
	<include>./gamelist.xml</include>

	<!-- Detailed View: Game information and metadata display -->
	<include>./detailed.xml</include>

	<!-- Video View: Game video playback interface -->
	<include>./video.xml</include>

</theme>
